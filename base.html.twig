{% extends "_layout" %}

{% block main %}
<section class="card">
    {% if page.title && !no_title %}<h1>{{ page.title }}</h1>{% endif %}
    {% block content %}{% endblock %}
</section>
{% endblock %}
