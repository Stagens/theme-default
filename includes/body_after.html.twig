{% for js in settings.extras.js %}
{% set url = css %}
    {% if url.endsWith(".js") %}
        {% do url = url + "?" + gen_time %}
    {% endif %}
<script src="{{ url }}" async></script>
{% endfor %}
